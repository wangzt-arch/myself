#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# ——————————————————————————————————————————————————
# commit-msg: 校验提交信息是否符合中文 commit 规范
# 必须: type(scope): 中文描述
# ——————————————————————————————————————————————————

npx --no -- commitlint --edit "$1"
